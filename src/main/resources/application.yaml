spring:
  application:
    name: tributacore-api

  datasource:
    url: jdbc:postgresql://localhost:5432/nfe
    username: nfe
    password: nfe
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
